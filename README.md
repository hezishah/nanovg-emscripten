# nanovg-emscripten

In order to build for web:

1. Configure emscripten environment
2. Run the following command:
> ./build_example_emscripten.sh example_gles2.c
3. Load index.html in the browser or start node webkit
